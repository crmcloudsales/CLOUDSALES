import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "npm:@supabase/supabase-js@2";
const U=Deno.env.get("SUPABASE_URL")!,A=Deno.env.get("SUPABASE_ANON_KEY")!,S=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!;
const ORIGINS=new Set(["https://cloudsales.app","https://www.cloudsales.app","https://app.cloudsales.app","http://localhost:3000","http://localhost:5173"]);
function cors(o:string|null){const v=o&&ORIGINS.has(o)?o:"https://app.cloudsales.app";return{"Access-Control-Allow-Origin":v,"Access-Control-Allow-Headers":"authorization,apikey,content-type,x-client-info","Access-Control-Allow-Methods":"POST,OPTIONS","Vary":"Origin"}}
function json(b:unknown,s=200,o:string|null=null){return new Response(JSON.stringify(b),{status:s,headers:{...cors(o),"content-type":"application/json;charset=utf-8","cache-control":"no-store","x-content-type-options":"nosniff"}})}
async function sha(v:string){const d=await crypto.subtle.digest("SHA-256",new TextEncoder().encode(v));return[...new Uint8Array(d)].map(x=>x.toString(16).padStart(2,"0")).join("")}
const cleanId=(v:any)=>String(v??"").replace(/\D/g,"");
Deno.serve(async req=>{const o=req.headers.get("origin");if(req.method==="OPTIONS")return new Response(null,{status:204,headers:cors(o)});if(req.method!=="POST")return json({error:"method_not_allowed"},405,o);if(o&&!ORIGINS.has(o))return json({error:"origin_not_allowed"},403,o);const auth=req.headers.get("authorization");if(!auth)return json({error:"missing_authorization"},401,o);const uc=createClient(U,A,{global:{headers:{Authorization:auth}},auth:{persistSession:false,autoRefreshToken:false}});const {data:ud}=await uc.auth.getUser();const user=ud.user;if(!user)return json({error:"invalid_session"},401,o);let b:any;try{b=await req.json()}catch{return json({error:"invalid_json"},400,o)}const org=String(b.organization_id||""),provider=String(b.provider_key||""),attemptId=String(b.oauth_attempt_id||""),state=String(b.state||"");if(!org||!provider||!attemptId||!state)return json({error:"missing_required_fields"},400,o);const svc=createClient(U,S,{auth:{persistSession:false,autoRefreshToken:false}});const {data:m}=await svc.from("organization_members").select("role,status").eq("organization_id",org).eq("user_id",user.id).maybeSingle();if(!m||m.status!=="active"||!["owner","admin"].includes(String(m.role)))return json({error:"forbidden"},403,o);const stateHash=await sha(state);const {data:a}=await svc.from("oauth_states").select("id,state_hash,organization_id,provider_key,user_id,redirect_to,expires_at,consumed_at,callback_code_secret_id,callback_error,metadata").eq("id",attemptId).eq("organization_id",org).eq("provider_key",provider).eq("user_id",user.id).maybeSingle();if(!a||a.state_hash!==stateHash||a.consumed_at||new Date(a.expires_at).getTime()<=Date.now())return json({error:"invalid_oauth_attempt"},400,o);if(a.callback_error)return json({error:"provider_callback_error",detail:a.callback_error},422,o);let code=String(b.code||"").trim();if(!code&&a.callback_code_secret_id){const {data}=await svc.rpc("service_read_secret",{p_secret_id:a.callback_code_secret_id});if(data)code=String(data)}if(!code)return json({error:"oauth_code_not_received"},409,o);
if(provider!=="google_ads"){const target=provider==="highlevel"?"connect-highlevel":"connection-complete-v3";const payload=provider==="highlevel"?{organization_id:org,oauth_attempt_id:attemptId,code,redirect_uri:a.redirect_to||b.redirect_uri||null,user_type:b.user_type||"Location"}:{...b,organization_id:org,provider_key:provider,oauth_attempt_id:attemptId,state,code};const r=await fetch(`${U}/functions/v1/${target}`,{method:"POST",headers:{Authorization:auth,apikey:A,"content-type":"application/json",...(o?{origin:o}:{})},body:JSON.stringify(payload)});const txt=await r.text();if(r.ok&&a.callback_code_secret_id){await svc.rpc("service_update_secret",{p_secret_id:a.callback_code_secret_id,p_secret:crypto.randomUUID()+crypto.randomUUID(),p_name:`cloudsales/oauth/${attemptId}/consumed`,p_description:"Consumed OAuth authorization code"})}if(r.ok)await svc.from("audit_log").insert({organization_id:org,actor_user_id:user.id,actor_type:"user",action:"connection.oauth.completed.v4",entity_type:"oauth_state",entity_id:attemptId,success:true,context:{provider_key:provider,handler:target}});return new Response(txt,{status:r.status,headers:{...cors(o),"content-type":"application/json;charset=utf-8","cache-control":"no-store"}})}
const [{data:cred},{data:devSetting}]=await Promise.all([svc.from("provider_app_credentials").select("client_id,client_secret_secret_id,redirect_uri,enabled,metadata").eq("provider_key","google_ads").maybeSingle(),svc.from("internal_settings").select("secret_id").eq("setting_key","google_ads_developer_token").maybeSingle()]);if(!cred?.enabled||!cred.client_id||!cred.client_secret_secret_id||!cred.redirect_uri||!devSetting?.secret_id)return json({error:"google_ads_platform_not_configured"},503,o);if(a.redirect_to&&a.redirect_to!==cred.redirect_uri)return json({error:"redirect_uri_mismatch"},400,o);const [{data:clientSecret},{data:developerToken}]=await Promise.all([svc.rpc("service_read_secret",{p_secret_id:cred.client_secret_secret_id}),svc.rpc("service_read_secret",{p_secret_id:devSetting.secret_id})]);if(!clientSecret||!developerToken)return json({error:"google_ads_platform_secret_unavailable"},503,o);const f=new URLSearchParams({grant_type:"authorization_code",client_id:String(cred.client_id),client_secret:String(clientSecret),redirect_uri:String(cred.redirect_uri),code});const tr=await fetch("https://oauth2.googleapis.com/token",{method:"POST",headers:{"content-type":"application/x-www-form-urlencoded",accept:"application/json"},body:f});const token=await tr.json().catch(()=>({}));if(!tr.ok||!token.access_token){await svc.from("audit_log").insert({organization_id:org,actor_user_id:user.id,actor_type:"user",action:"connection.oauth.failed",entity_type:"oauth_state",entity_id:attemptId,success:false,context:{provider_key:"google_ads",stage:"token_exchange",status:tr.status}});return json({error:"google_ads_oauth_exchange_failed",status:tr.status},502,o)}const ar=await fetch("https://googleads.googleapis.com/v25/customers:listAccessibleCustomers",{headers:{Authorization:`Bearer ${token.access_token}`,"developer-token":String(developerToken),accept:"application/json"}});const accessible=await ar.json().catch(()=>({}));if(!ar.ok||accessible?.error){await svc.from("audit_log").insert({organization_id:org,actor_user_id:user.id,actor_type:"user",action:"connection.oauth.failed",entity_type:"oauth_state",entity_id:attemptId,success:false,context:{provider_key:"google_ads",stage:"api_validation",status:ar.status}});return json({error:"google_ads_api_validation_failed",status:ar.status,detail:String(accessible?.error?.message||"").slice(0,400)},502,o)}const ids=(accessible.resourceNames||[]).map((x:string)=>cleanId(x)).filter(Boolean);if(!ids.length)return json({error:"google_ads_no_accessible_customers"},409,o);const externalId=`google_ads_user:${user.id}`;const expiresAt=new Date(Date.now()+Number(token.expires_in||3600)*1000).toISOString();const scopes=String(token.scope||"https://www.googleapis.com/auth/adwords").split(/\s+/).filter(Boolean);const metadata={api_version:"v25",oauth_flow:"multi_user",accessible_customer_ids:ids,billing_mode:"provider_managed",billing_portal_url:"https://ads.google.com/",selected_customer_id:null,connected_at:new Date().toISOString()};let connectionId="";const {data:existing}=await svc.from("connections").select("id").eq("organization_id",org).eq("provider_key","google_ads").eq("external_account_id",externalId).maybeSingle();if(existing?.id){connectionId=existing.id;const {error}=await svc.from("connections").update({status:"connected",external_account_name:"Google Ads",scopes,expires_at:expiresAt,last_sync_at:new Date().toISOString(),metadata}).eq("id",connectionId);if(error)return json({error:"connection_update_failed"},500,o)}else{const {data:c,error}=await svc.from("connections").insert({organization_id:org,provider_key:"google_ads",external_account_id:externalId,external_account_name:"Google Ads",status:"connected",scopes,expires_at:expiresAt,last_sync_at:new Date().toISOString(),metadata,created_by:user.id}).select("id").single();if(error||!c)return json({error:"connection_create_failed"},500,o);connectionId=c.id}const {data:es}=await svc.from("connection_secrets").select("access_token_secret_id,refresh_token_secret_id").eq("connection_id",connectionId).maybeSingle();let accessId=es?.access_token_secret_id,refreshId=es?.refresh_token_secret_id;if(accessId)await svc.rpc("service_update_secret",{p_secret_id:accessId,p_secret:String(token.access_token),p_name:`cloudsales/google_ads/${connectionId}/access`,p_description:"Google Ads OAuth access token"});else{const {data:id}=await svc.rpc("service_store_secret",{p_secret:String(token.access_token),p_name:`cloudsales/google_ads/${connectionId}/access`,p_description:"Google Ads OAuth access token"});accessId=id}if(token.refresh_token){if(refreshId)await svc.rpc("service_update_secret",{p_secret_id:refreshId,p_secret:String(token.refresh_token),p_name:`cloudsales/google_ads/${connectionId}/refresh`,p_description:"Google Ads OAuth refresh token"});else{const {data:id}=await svc.rpc("service_store_secret",{p_secret:String(token.refresh_token),p_name:`cloudsales/google_ads/${connectionId}/refresh`,p_description:"Google Ads OAuth refresh token"});refreshId=id}}if(!accessId||!refreshId)return json({error:"google_ads_token_storage_failed",refresh_token_received:Boolean(token.refresh_token)},500,o);await svc.from("connection_secrets").upsert({connection_id:connectionId,access_token_secret_id:accessId,refresh_token_secret_id:refreshId,rotated_at:new Date().toISOString()},{onConflict:"connection_id"});await svc.from("oauth_states").update({consumed_at:new Date().toISOString(),metadata:{...(a.metadata||{}),completed:true,api_version:"v25"}}).eq("id",attemptId);if(a.callback_code_secret_id)await svc.rpc("service_update_secret",{p_secret_id:a.callback_code_secret_id,p_secret:crypto.randomUUID()+crypto.randomUUID(),p_name:`cloudsales/oauth/${attemptId}/consumed`,p_description:"Consumed OAuth authorization code"});await svc.from("audit_log").insert({organization_id:org,actor_user_id:user.id,actor_type:"user",action:"connection.oauth.connected",entity_type:"connection",entity_id:connectionId,connection_id:connectionId,success:true,context:{provider_key:"google_ads",accessible_customers:ids.length,api_version:"v25"}});return json({connection:{id:connectionId,provider_key:"google_ads",status:"connected",external_account_name:"Google Ads",expires_at:expiresAt,scopes,accessible_customer_count:ids.length}},200,o)});
