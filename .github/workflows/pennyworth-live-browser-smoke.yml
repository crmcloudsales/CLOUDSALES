name: Pennyworth Live Browser Smoke
# Verify live contact gates and automatic localization

on:
  push:
    branches: [main]
    paths:
      - 'web/clients/pennyworth/**'
      - '.github/workflows/pennyworth-live-browser-smoke.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  live-browser:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install Playwright Chromium
        run: |
          npm install --no-save playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Verify Pennyworth live contact gates and i18n
        shell: bash
        run: |
          node <<'NODE'
          const { chromium } = require('playwright');
          (async()=>{
            const browser = await chromium.launch({headless:true});
            async function check(lang, expected, dir='ltr'){
              const page = await browser.newPage({viewport:{width:390,height:844}, locale:lang});
              const errors=[]; page.on('pageerror',e=>errors.push(String(e.message||e)));
              const url='https://pennyworth.cloudsales.app/?lang='+encodeURIComponent(lang)+'&qa='+Date.now();
              const r=await page.goto(url,{waitUntil:'networkidle',timeout:45000});
              if(!r||!r.ok()) throw new Error(lang+'_http_'+(r&&r.status()));
              await page.waitForTimeout(1200);
              const htmlLang=await page.locator('html').getAttribute('lang');
              const htmlDir=await page.locator('html').getAttribute('dir');
              const hero=(await page.locator('.hero h1').innerText()).trim();
              const marker=await page.locator('html').getAttribute('data-i18n');
              console.log('I18N_'+lang+'='+JSON.stringify({htmlLang,htmlDir,hero,marker,errors}));
              if(errors.length) throw new Error(lang+'_page_errors:'+errors.join('|'));
              if(hero!==expected) throw new Error(lang+'_hero_not_translated:'+hero);
              if(htmlDir!==dir) throw new Error(lang+'_dir_'+htmlDir);
              if(marker!=='pennyworth-i18n-v1') throw new Error(lang+'_marker_missing');
              const chat=page.locator('#pwChatLaunch'),wa=page.locator('#pwWaLaunch');
              await chat.waitFor({state:'visible',timeout:5000}); await wa.waitFor({state:'visible',timeout:5000});
              await chat.click(); const cp=page.locator('#pwPanel-chat'); await cp.waitFor({state:'visible',timeout:5000});
              await cp.locator('.pwSubmit').waitFor({state:'visible',timeout:3000}); await cp.locator('.pwClose').click();
              await wa.click(); const wp=page.locator('#pwPanel-whatsapp'); await wp.waitFor({state:'visible',timeout:5000});
              await wp.locator('.pwSubmit').waitFor({state:'visible',timeout:3000}); await wp.locator('.pwClose').click();
              await page.close();
            }
            await check('en-US','We redefine the investment experience.');
            await check('ar-AE','نعيد تعريف تجربة الاستثمار.','rtl');
            console.log('PENNYWORTH_I18N_AND_CONTACT_GATES_PASS');
            await browser.close();
          })().catch(e=>{console.error(e);process.exit(1)});
          NODE
