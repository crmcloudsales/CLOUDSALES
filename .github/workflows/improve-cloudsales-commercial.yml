name: Improve CloudSales Commercial Site

on:
  push:
    paths:
      - "scripts/improve_cloudsales_commercial.py"
      - ".github/workflows/improve-cloudsales-commercial.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch-commercial:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Apply UX patch
        run: python scripts/improve_cloudsales_commercial.py

      - name: Validate commercial source
        run: |
          python - <<'PY'
          from pathlib import Path
          s=Path('web/commercial.html').read_text(encoding='utf-8')
          runtime=Path('web/commercial-sales-story-v1.js').read_text(encoding='utf-8')
          i18n=Path('web/cloudsales-i18n-v1.js').read_text(encoding='utf-8')
          checks={
            'existing_plans_preserved':'class="plans"' in s,
            'sales_story_inlined':'id="cs-commercial-sales-story-v1"' in s,
            'no_external_story_dependency':'src="/commercial-sales-story-v1.js' not in s,
            'trial_checkout':'/subscribe?plan=pro' in runtime,
            'trial_copy':'7 DÍAS CLOUDY GRATIS' in runtime,
            'no_stale_spanish_14':'14 días' not in (s+i18n).lower(),
            'no_stale_english_14':'14 days' not in (s+i18n).lower(),
            'lead_quality':'Junk Lead Firewall' in runtime,
            'continuous_improvement':'MEJORA CONTINUA' in runtime,
            'agentcloud':'AGENTCLOUD' in runtime,
            'app_value':'¿QUÉ HACE LA APP?' in runtime,
          }
          print(checks)
          assert all(checks.values()), checks
          PY

      - name: Commit commercial update
        run: |
          TARGETS="web/commercial.html web/cloudsales-i18n-v1.js web/commercial-brand-runtime-v2.js web/commercial/conversion-v1.js"
          if git diff --quiet -- $TARGETS; then
            echo "Commercial site already up to date"
            exit 0
          fi
          git config user.name "cloudsales-build-bot"
          git config user.email "cloudsales-build-bot@users.noreply.github.com"
          git add $TARGETS
          git commit -m "feat(site): Cloudy conversion story and canonical 7-day trial"
          git push
