name: Run PWA auth gate fix
on:
  push:
    branches: [main]
    paths:
      - 'scripts/pwa-auth-gate-fix-20260905.js'
      - '.github/workflows/run-pwa-auth-gate-fix-20260905.yml'
permissions:
  contents: write
jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node scripts/pwa-auth-gate-fix-20260905.js
      - run: |
          git config user.name crmcloudsales
          git config user.email crmcloudsales@gmail.com
          git add web/native-shell-runtime-v1.js
          git diff --cached --quiet && exit 0
          git commit -m "fix(pwa): validate cached session before private shell"
          git push
