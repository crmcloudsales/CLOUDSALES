const fs=require('fs');const p='web/native-shell-runtime-v1.js';let s=fs.readFileSync(p,'utf8');const start=s.indexOf('async function hardAuthGate()');const end=s.indexOf('\nfunction enforceCanonicalTypography()',start);if(start<0||end<0)throw Error('hardAuthGate block missing');const fn=`async function hardAuthGate(){try{if(typeof session==='undefined'||!session?.access_token){try{typeof clearSession==='function'&&clearSession()}catch{};try{typeof showAuth==='function'&&showAuth()}catch{};return false}if(typeof api!=='function')return true;await api('app-state',{});return true}catch(e){if(Number(e?.status)===401||/invalid_session|missing_authorization|session_required/i.test(String(e?.message||''))){try{typeof clearSession==='function'&&clearSession()}catch{};try{typeof showAuth==='function'&&showAuth()}catch{};return false}return true}}`;s=s.slice(0,start)+fn+s.slice(end);fs.writeFileSync(p,s);if(!s.includes("await api('app-state',{})"))throw Error('auth gate assertion failed');console.log('AUTH_GATE_BFF_PASS');